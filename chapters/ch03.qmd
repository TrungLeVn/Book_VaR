# Đo lường và dự báo biến động tỷ lệ sinh lời của VN-Index

Chương 2 đã trình bày các nhóm phương pháp đo lường và mô hình hóa volatility, bao gồm historical volatility, rolling volatility, EWMA/RiskMetrics, ARCH/GARCH, các mô hình bất đối xứng và các thước đo volatility dựa trên dữ liệu OHLC hoặc tần suất cao. Chương này chuyển từ phần phương pháp sang phần ứng dụng thực nghiệm cho thị trường chứng khoán Việt Nam, với trọng tâm là VN-Index.

Mục tiêu của chương không phải là giới thiệu lại toàn bộ lý thuyết ở Chương 2, mà là triển khai một quy trình thực nghiệm nhất quán: chuẩn bị dữ liệu, kiểm định đặc điểm chuỗi tỷ lệ sinh lời, đo lường volatility bằng nhiều phương pháp, ước lượng các mô hình GARCH-type, dự báo volatility theo nhiều horizon và so sánh hiệu quả dự báo. Cách tổ chức này giúp người đọc thấy rõ mô hình nào phản ứng nhanh với cú sốc thị trường, mô hình nào ổn định hơn, và mô hình nào phù hợp hơn với các mục tiêu quản trị rủi ro ngắn hạn [@Tsay2010; @Tsay2013; @Brooks2019; @Choudhry2013].

```{r}
#| label: setup-ch03
#| include: false

pkgs <- c(
  "ggplot2", "dplyr", "tidyr", "zoo", "knitr", "scales",
  "tibble", "readxl", "FinTS", "rugarch",
  "tseries", "moments"
)

missing_pkgs <- pkgs[!vapply(pkgs, requireNamespace, logical(1), quietly = TRUE)]

if (length(missing_pkgs) > 0) {
  stop(
    "Cần cài các package sau trước khi render: ",
    paste(missing_pkgs, collapse = ", ")
  )
}

invisible(lapply(pkgs, library, character.only = TRUE))

knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  dev = "cairo_pdf"
)

base_family <- "sans"
base_size <- 13

theme_book <- function() {
  theme_minimal(base_family = base_family, base_size = base_size) +
    theme(
      plot.title = element_text(face = "bold", size = 15, margin = margin(b = 4)),
      plot.subtitle = element_text(size = 12, margin = margin(b = 10)),
      axis.title = element_text(size = 12),
      axis.text = element_text(size = 11),
      legend.position = "bottom",
      legend.title = element_text(size = 11),
      legend.text = element_text(size = 10),
      panel.grid.minor = element_blank()
    )
}



# Bảng màu và kiểu đường dùng thống nhất cho toàn bộ Chương 3.
# Các màu được chọn theo hướng tương phản cao và thân thiện với bản in PDF.
book_palette <- c(
  "#0072B2", # blue
  "#D55E00", # vermillion
  "#009E73", # green
  "#CC79A7", # purple
  "#E69F00", # orange
  "#56B4E9", # sky blue
  "#000000", # black
  "#999999"  # grey
)

book_linetypes <- c(
  "solid", "dashed", "dotdash", "twodash",
  "longdash", "dotted", "solid", "dashed"
)

book_color <- function(i) book_palette[((i - 1) %% length(book_palette)) + 1]

label_pct <- function(accuracy = 0.01) {
  scales::label_percent(accuracy = accuracy, decimal.mark = ",")
}

fmt_pct <- function(x, accuracy = 0.01) {
  scales::percent(x, accuracy = accuracy, decimal.mark = ",")
}

fmt_num <- function(x, digits = 3) {
  format(round(x, digits), big.mark = ".", decimal.mark = ",", nsmall = digits)
}

roll_sd <- function(x, width) {
  zoo::rollapplyr(x, width = width, FUN = sd, fill = NA)
}

roll_var <- function(x, width) {
  zoo::rollapplyr(x, width = width, FUN = var, fill = NA)
}

ewma_vol <- function(x, lambda = 0.94) {
  sigma2 <- numeric(length(x))
  sigma2[1] <- var(x, na.rm = TRUE)

  for (i in 2:length(x)) {
    sigma2[i] <- lambda * sigma2[i - 1] + (1 - lambda) * x[i - 1]^2
  }

  sqrt(sigma2)
}

make_acf_df <- function(x, label, lag_max = 30) {
  ac <- stats::acf(x, lag.max = lag_max, plot = FALSE, na.action = na.pass)

  tibble(
    lag = as.numeric(ac$lag),
    acf = as.numeric(ac$acf),
    series = label
  ) |>
    filter(lag > 0)
}

safe_test <- function(expr) {
  tryCatch(expr, error = function(e) NULL, warning = function(w) suppressWarnings(expr))
}

safe_stat <- function(test_obj) {
  if (is.null(test_obj)) return(NA_real_)
  round(unname(test_obj$statistic[1]), 3)
}

safe_pvalue <- function(test_obj) {
  if (is.null(test_obj)) return(NA_real_)
  signif(test_obj$p.value, 4)
}

make_garch_spec <- function(model = "sGARCH", dist = "std") {
  rugarch::ugarchspec(
    variance.model = list(model = model, garchOrder = c(1, 1)),
    mean.model = list(armaOrder = c(0, 0), include.mean = TRUE),
    distribution.model = dist
  )
}

fit_garch_safe <- function(x, model = "sGARCH", dist = "std") {
  spec <- make_garch_spec(model = model, dist = dist)
  tryCatch(
    rugarch::ugarchfit(spec = spec, data = x, solver = "hybrid"),
    error = function(e) NULL
  )
}

coef_table <- function(fit, model_name = "Mô hình") {
  if (is.null(fit)) {
    return(tibble(
      `Mô hình` = model_name,
      `Tham số` = NA_character_,
      `Ước lượng` = NA_real_,
      `Sai số chuẩn` = NA_real_,
      `p-value` = NA_real_
    ))
  }

  mat <- fit@fit$matcoef

  tibble(
    `Mô hình` = model_name,
    `Tham số` = rownames(mat),
    `Ước lượng` = round(mat[, 1], 6),
    `Sai số chuẩn` = round(mat[, 2], 6),
    `p-value` = signif(mat[, 4], 4)
  )
}



sig_stars <- function(p) {
  ifelse(
    is.na(p), "",
    ifelse(p < 0.01, "***", ifelse(p < 0.05, "**", ifelse(p < 0.10, "*", "")))
  )
}

fmt_coef_num <- function(x, digits = 6) {
  ifelse(
    is.na(x), "",
    formatC(x, digits = digits, format = "f", big.mark = ".", decimal.mark = ",")
  )
}

format_coef_cell <- function(fit, param) {
  if (is.null(fit)) return("")
  mat <- fit@fit$matcoef
  if (!(param %in% rownames(mat))) return("")

  estimate <- mat[param, 1]
  se <- mat[param, 2]
  p_value <- mat[param, 4]

  paste0(
    fmt_coef_num(estimate),
    sig_stars(p_value),
    " (", fmt_coef_num(se), ")"
  )
}

parameter_label <- function(x) {
  dplyr::recode(
    x,
    mu = "Mean: mu",
    ar1 = "Mean: ar1",
    ma1 = "Mean: ma1",
    omega = "Variance: omega",
    alpha1 = "ARCH: alpha1",
    beta1 = "GARCH: beta1",
    gamma1 = "Asymmetry: gamma1",
    eta1 = "Asymmetry: eta1",
    delta = "Power: delta",
    shape = "Distribution: shape",
    skew = "Distribution: skew",
    .default = x
  )
}

model_short_name <- function(x) {
  dplyr::recode(
    x,
    `GARCH-normal` = "GARCH-N",
    `GARCH-Student-t` = "GARCH-t",
    `GJR-GARCH-Student-t` = "GJR-t",
    `EGARCH-Student-t` = "EGARCH-t",
    .default = x
  )
}

garch_journal_table <- function(model_fits) {
  preferred_order <- c(
    "mu", "ar1", "ma1", "omega", "alpha1", "beta1",
    "gamma1", "eta1", "delta", "shape", "skew"
  )

  available_params <- unique(unlist(lapply(model_fits, function(fit) {
    if (is.null(fit)) return(character(0))
    rownames(fit@fit$matcoef)
  })))

  params <- c(
    preferred_order[preferred_order %in% available_params],
    setdiff(available_params, preferred_order)
  )

  out <- tibble(`Tham số` = parameter_label(params))

  for (model_name in names(model_fits)) {
    out[[model_short_name(model_name)]] <- vapply(
      params,
      function(param) format_coef_cell(model_fits[[model_name]], param),
      character(1)
    )
  }

  out
}

ic_table <- function(fit, model_name = "Mô hình") {
  if (is.null(fit)) {
    return(tibble(
      `Mô hình` = model_name,
      AIC = NA_real_,
      BIC = NA_real_,
      `Hannan-Quinn` = NA_real_
    ))
  }

  ic <- rugarch::infocriteria(fit)

  tibble(
    `Mô hình` = model_name,
    AIC = round(as.numeric(ic["Akaike"]), 5),
    BIC = round(as.numeric(ic["Bayes"]), 5),
    `Hannan-Quinn` = round(as.numeric(ic["Hannan-Quinn"]), 5)
  )
}

qlike_loss <- function(actual_var, forecast_var) {
  eps <- 1e-10
  actual_var <- pmax(actual_var, eps)
  forecast_var <- pmax(forecast_var, eps)
  mean(log(forecast_var) + actual_var / forecast_var, na.rm = TRUE)
}
```


## Tổng quan nghiên cứu về dự báo volatility và định vị thực nghiệm của chương

Chương 2 đã trình bày các mô hình volatility theo logic phương pháp. Trước khi áp dụng cho VN-Index, cần xác lập cơ sở nghiên cứu thực nghiệm để trả lời ba câu hỏi: vì sao cần so sánh nhiều mô hình, vì sao cần xét cả mô hình đối xứng và bất đối xứng, và vì sao đánh giá dự báo volatility không nên chỉ dựa vào một tiêu chí đơn lẻ. Phần tổng quan này không nhằm thay thế lý thuyết của Chương 2, mà làm rõ cách các nghiên cứu thực nghiệm gần đây thiết kế bài toán forecasting comparison trong bối cảnh thị trường mới nổi.

Trước hết, các nghiên cứu về thị trường mới nổi thường bắt đầu từ ba đặc điểm thực nghiệm của tỷ lệ sinh lời chứng khoán: phân phối không chuẩn, volatility clustering và phản ứng bất đối xứng trước thông tin xấu. Gupta [-@Gupta2025] so sánh GARCH, EGARCH và GJR-GARCH cho các thị trường G4 trong nhiều giai đoạn khủng hoảng và cho rằng mô hình bất đối xứng, đặc biệt EGARCH, thường phù hợp hơn khi tác động của cú sốc âm và cú sốc dương lên volatility không giống nhau. Tuy nhiên, cùng nghiên cứu này cũng ghi nhận bằng chứng trong tài liệu trước đó rằng GARCH(1,1) đối xứng vẫn có thể là lựa chọn cạnh tranh trong một số thị trường và tài sản, hàm ý rằng không nên mặc định mô hình bất đối xứng luôn vượt trội.

Kết luận không thống nhất này cũng xuất hiện trong các nghiên cứu thị trường mới nổi khác. Adegboyo và Sarwar [-@AdegboyoSarwar2025] phân tích thị trường chứng khoán Nigeria và nhấn mạnh sự hiện diện của volatility clustering, leverage effect và nhu cầu sử dụng các phân phối phi chuẩn như Student-t để xử lý đuôi dày. Trong nghiên cứu này, GJR-GARCH với phân phối Student-t cho kết quả tốt nhất về dự báo volatility. Cách tiếp cận đó gợi ý rằng nghiên cứu thực nghiệm cho VN-Index nên bao gồm ít nhất một mô hình đối xứng làm benchmark, một hoặc hai mô hình bất đối xứng, và lựa chọn phân phối Student-t bên cạnh phân phối chuẩn.

Một điểm quan trọng khác là mô hình phức tạp hơn không luôn dự báo tốt hơn. Ding và Meade [-@DingMeade2010] so sánh EWMA, GARCH và stochastic volatility trong các kịch bản volatility khác nhau. Kết quả cho thấy EWMA có thể đạt độ chính xác đáng tin cậy trong nhiều bối cảnh, đặc biệt khi volatility-of-volatility ở mức trung bình; SV chỉ vượt trội rõ rệt trong những trường hợp đặc biệt có volatility-of-volatility rất cao. Điều này cung cấp cơ sở để đưa EWMA vào Chương 3 không chỉ như mô hình đơn giản, mà như một benchmark thực nghiệm nghiêm túc khi so sánh forecast volatility.

Các nghiên cứu gần đây về risk forecasting cũng nhấn mạnh vai trò của benchmark đơn giản. Alexander và Dakos [-@AlexanderDakos2023] cho thấy các mô hình EWMA kiểu RiskMetrics có thể đạt kết quả cạnh tranh với GARCH trong dự báo VaR/ES và volatility, nhất là khi mô hình cho phép phản ứng bất đối xứng và phân phối đuôi dày. Nghiên cứu này cũng chỉ ra rằng tài liệu học thuật thường quá tập trung vào các mô hình GARCH phức tạp trong khi thực hành quản trị rủi ro lại cần mô hình dễ giải thích, dễ kiểm định và ổn định khi dự báo ngoài mẫu. Đối với một cuốn sách ứng dụng cho thị trường Việt Nam, hàm ý là không nên biến Chương 3 thành một ``model zoo''. Thay vào đó, nên so sánh một tập mô hình vừa đủ: rolling volatility, EWMA, GARCH-normal, GARCH-Student-t, GJR-GARCH-Student-t và EGARCH-Student-t.

Về cách đánh giá mô hình, các nghiên cứu journal-style thường kết hợp tiêu chí trong mẫu và ngoài mẫu. Trong mẫu, AIC/BIC và kiểm định phần dư chuẩn hóa giúp đánh giá độ phù hợp thống kê. Ngoài mẫu, các loss functions như RMSE, MAE và QLIKE được dùng để so sánh forecast volatility, trong khi các nghiên cứu VaR/ES thường bổ sung Kupiec, Christoffersen hoặc Dynamic Quantile tests. Alexander và Dakos [-@AlexanderDakos2023] tổng hợp rằng các nghiên cứu forecasting thường sử dụng AIC/BIC, kiểm định phần dư, RMSE, MAE, QLIKE, MCS, Diebold--Mariano và các scoring rules. Likitratcharoen và Suwannamalik [-@LikitratcharoenSuwannamalik2024] cũng sử dụng rolling forecast và so sánh các mô hình VaR/GARCH-type trong giai đoạn COVID-19, qua đó cho thấy cách tổ chức thực nghiệm cần phản ánh khả năng thích nghi của mô hình trong các giai đoạn thị trường căng thẳng.

Từ tổng quan trên, Chương 3 được thiết kế theo hướng thực nghiệm có kiểm soát. Thứ nhất, các đặc điểm dữ liệu VN-Index được kiểm tra trước khi mô hình hóa, bao gồm tính dừng, phân phối đuôi dày, tự tương quan và ARCH effect. Thứ hai, các mô hình được chia thành ba nhóm: mô hình benchmark đơn giản, mô hình EWMA/RiskMetrics và mô hình GARCH-type. Thứ ba, forecasting comparison được thực hiện theo nhiều horizon, gồm 1, 5, 10 và 20 ngày, để đánh giá xem mô hình nào phù hợp hơn với rủi ro rất ngắn hạn, theo tuần, theo chuẩn 10 ngày và theo tháng giao dịch. Thứ tư, kết quả forecast được trình bày theo phong cách bảng journal-style, trong đó mỗi tiêu chí sai số là một panel riêng, giúp người đọc so sánh mô hình theo horizon mà không bị nhiễu bởi quá nhiều con số trong một bảng duy nhất.

| Hướng nghiên cứu | Hàm ý cho Chương 3 |
|---|---|
| GARCH đối xứng | Benchmark nền |
| GARCH bất đối xứng | Leverage effect |
| Student-t | Đuôi dày |
| EWMA | Benchmark thực hành |
| Rolling forecast | Out-of-sample |
| RMSE, MAE, QLIKE | Forecast comparison |

Tóm lại, tổng quan nghiên cứu cho thấy không có một mô hình volatility forecasting tốt nhất cho mọi thị trường, mọi giai đoạn và mọi horizon. Vì vậy, trọng tâm của Chương 3 không phải là chứng minh một mô hình luôn vượt trội, mà là xây dựng một quy trình thực nghiệm minh bạch để so sánh mô hình, đánh giá tính ổn định của kết quả và rút ra hàm ý phù hợp cho đo lường rủi ro thị trường chứng khoán Việt Nam.

## Dữ liệu và thiết kế thực nghiệm

Dữ liệu sử dụng trong chương là chuỗi VN-Index với tần suất ngày. Về nguyên tắc, dữ liệu tối thiểu cần có giá đóng cửa để tính tỷ lệ sinh lời. Trong chương này, file dữ liệu nội bộ được lưu tại `data/vni_data.xlsx` trong thư mục gốc của project. File có các cột `Date`, `Price`, `Open`, `High` và `Low`, trong đó `Price` được sử dụng như giá đóng cửa để tính log return. Các cột `Open`, `High` và `Low` được dùng để tính các thước đo range-based volatility như Parkinson và Garman--Klass.

Việc dùng file Excel nội bộ giúp chương có tính tái lập cao hơn so với việc tải dữ liệu trực tuyến. Khi chuyển project sang máy khác, chỉ cần giữ đúng cấu trúc thư mục `data/vni_data.xlsx` là toàn bộ code trong chương có thể chạy lại mà không phụ thuộc vào kết nối Internet hay thay đổi mã dữ liệu trên các nền tảng bên ngoài.

```{r}
#| label: load-vnindex-data
#| warning: false
#| message: false

local_data_path <- "../data/vni_data.xlsx"

if (!file.exists(local_data_path)) {
  stop(
    "Không tìm thấy file dữ liệu. ",
    "Hãy lưu file Excel tại: data/vni_data.xlsx. ",
    "File cần có các cột Date, Price, Open, High và Low."
  )
}

raw_data <- readxl::read_excel(local_data_path)
names(raw_data) <- trimws(names(raw_data))

find_col <- function(candidates, data_names = names(raw_data)) {
  idx <- match(tolower(candidates), tolower(data_names), nomatch = 0)
  idx <- idx[idx > 0]
  if (length(idx) == 0) return(NA_character_)
  data_names[idx[1]]
}

col_date  <- find_col(c("Date", "Ngay", "Ngày"))
col_close <- find_col(c("Price", "Close", "Adjusted", "Adj Close"))
col_open  <- find_col(c("Open"))
col_high  <- find_col(c("High"))
col_low   <- find_col(c("Low"))

required_map <- c(
  Date = col_date,
  Price_or_Close = col_close,
  Open = col_open,
  High = col_high,
  Low = col_low
)

missing_cols <- names(required_map)[is.na(required_map)]

if (length(missing_cols) > 0) {
  stop(
    "File data/vni_data.xlsx thiếu hoặc sai tên cột: ",
    paste(missing_cols, collapse = ", "),
    ". Tên cột kỳ vọng là Date, Price, Open, High, Low."
  )
}

parse_excel_date <- function(x) {
  if (inherits(x, "Date")) return(as.Date(x))
  if (inherits(x, "POSIXt")) return(as.Date(x))

  if (is.numeric(x)) {
    return(as.Date(x, origin = "1899-12-30"))
  }

  x_chr <- trimws(as.character(x))
  formats <- c("%Y-%m-%d", "%m/%d/%Y", "%d/%m/%Y", "%Y/%m/%d")

  out <- rep(as.Date(NA), length(x_chr))
  for (fmt in formats) {
    idx <- is.na(out)
    out[idx] <- as.Date(x_chr[idx], format = fmt)
  }

  out
}

clean_numeric <- function(x) {
  if (is.numeric(x)) return(as.numeric(x))
  as.numeric(gsub(",", "", trimws(as.character(x))))
}

# Trong file của anh, cột Price được sử dụng như giá đóng cửa.
df_price <- raw_data |>
  transmute(
    date = parse_excel_date(.data[[col_date]]),
    close = clean_numeric(.data[[col_close]]),
    open = clean_numeric(.data[[col_open]]),
    high = clean_numeric(.data[[col_high]]),
    low = clean_numeric(.data[[col_low]])
  ) |>
  filter(
    !is.na(date),
    is.finite(open), is.finite(high), is.finite(low), is.finite(close),
    open > 0, high > 0, low > 0, close > 0
  ) |>
  arrange(date) |>
  distinct(date, .keep_all = TRUE) |>
  mutate(
    log_price = log(close),
    return = log(close) - dplyr::lag(log_price)
  ) |>
  filter(is.finite(return))

if (nrow(df_price) < 500) {
  warning(
    "Số quan sát return nhỏ hơn 500. ",
    "Code vẫn chạy, nhưng kết quả GARCH và forecast có thể kém ổn định."
  )
}

if (any(df_price$high < df_price$low, na.rm = TRUE)) {
  stop("Dữ liệu không hợp lệ: tồn tại quan sát High nhỏ hơn Low.")
}

r_vnindex <- df_price$return

data_source_note <- paste(
  "Dữ liệu được đọc từ file nội bộ data/vni_data.xlsx;",
  "cột Price được sử dụng như giá đóng cửa."
)
```


```{r}
#| label: tbl-vnindex-data-info
#| tbl-cap: "Thông tin dữ liệu VN-Index sử dụng trong Chương 3"

sample_start <- min(df_price$date)
sample_end <- max(df_price$date)

info_table <- tibble(
  `Nội dung` = c(
    "Tài sản",
    "Tần suất",
    "Giai đoạn mẫu",
    "Số quan sát return",
    "Biến đầu vào",
    "Nguồn dữ liệu"
  ),
  `Thông tin` = c(
    "VN-Index",
    "Ngày giao dịch",
    paste0(format(sample_start, "%d/%m/%Y"), "--", format(sample_end, "%d/%m/%Y")),
    format(length(r_vnindex), big.mark = "."),
    "Log return từ giá đóng cửa; OHLC dùng cho range-based volatility",
    data_source_note
  )
)

info_table |>
  knitr::kable(align = "ll")
```

Trong chương này, tỷ lệ sinh lời được tính dưới dạng log return:

$$r_t = \log(P_t) - \log(P_{t-1}).$$

Việc sử dụng log return giúp thống nhất đầu vào cho các mô hình volatility và phù hợp với cách triển khai ở Chương 1 và Chương 2. Các mô hình được so sánh trong chương bao gồm historical volatility, rolling volatility, EWMA, GARCH, GJR-GARCH và EGARCH. Khi đánh giá dự báo nhiều horizon, chương sử dụng các horizon 1, 5, 10 và 20 ngày giao dịch. Các horizon này lần lượt đại diện cho dự báo ngắn hạn một ngày, một tuần, gần chuẩn 10 ngày trong quản trị rủi ro thị trường, và một tháng giao dịch.

```{r}
#| label: tbl-forecast-design
#| tbl-cap: "Thiết kế thực nghiệm dự báo volatility trong Chương 3"

forecast_design <- tibble(
  `Thành phần` = c(
    "Tài sản",
    "Tần suất",
    "Forecast horizons",
    "Benchmark đơn giản",
    "Mô hình chính",
    "Tiêu chí so sánh"
  ),
  `Thiết kế` = c(
    "VN-Index",
    "Ngày giao dịch",
    "1, 5, 10, 20 ngày",
    "Historical, rolling, EWMA",
    "GARCH, GJR-GARCH, EGARCH",
    "RMSE, MAE, QLIKE"
  )
)

forecast_design |>
  knitr::kable(align = "ll")
```

## Đặc điểm thống kê của tỷ lệ sinh lời VN-Index

Trước khi ước lượng các mô hình volatility, cần kiểm tra đặc điểm thống kê của chuỗi tỷ lệ sinh lời. Nếu chuỗi return có phân phối đuôi dày, tự tương quan trong bình phương return, hoặc ARCH effect, các mô hình volatility động sẽ có cơ sở thực nghiệm rõ ràng hơn. Đây là bước nối trực tiếp từ các stylized facts ở Chương 1 sang mô hình hóa volatility ở Chương 2.

```{r}
#| label: fig-vnindex-price-return
#| fig-cap: "Giá đóng cửa và tỷ lệ sinh lời logarit của VN-Index"
#| fig-width: 8
#| fig-height: 5.8

p1 <- ggplot(df_price, aes(x = date, y = close)) +
  geom_line(linewidth = 0.45, color = book_color(1)) +
  labs(
    title = "Giá đóng cửa VN-Index",
    x = "Thời gian",
    y = "Điểm chỉ số"
  ) +
  theme_book() +
  theme(legend.position = "none")

p2 <- ggplot(df_price, aes(x = date, y = return)) +
  geom_line(linewidth = 0.35, color = book_color(2)) +
  scale_y_continuous(labels = label_pct(accuracy = 1)) +
  labs(
    title = "Tỷ lệ sinh lời logarit ngày của VN-Index",
    x = "Thời gian",
    y = "Log return"
  ) +
  theme_book() +
  theme(legend.position = "none")

# Không dùng patchwork để giảm phụ thuộc package.
print(p1)
print(p2)
```

Hình @fig-vnindex-price-return cho thấy sự khác biệt giữa chuỗi giá và chuỗi tỷ lệ sinh lời. Chuỗi giá phản ánh xu hướng dài hạn của thị trường, trong khi chuỗi tỷ lệ sinh lời làm nổi bật các giai đoạn biến động cao và thấp. Đây là lý do các mô hình volatility thường được xây dựng trên return thay vì giá.

```{r}
#| label: tbl-vnindex-descriptive-stats
#| tbl-cap: "Thống kê mô tả tỷ lệ sinh lời ngày của VN-Index"

jb_test <- safe_test(tseries::jarque.bera.test(r_vnindex))

desc_table <- tibble(
  `Chỉ tiêu` = c(
    "Số quan sát",
    "Trung bình",
    "Độ lệch chuẩn",
    "Nhỏ nhất",
    "Lớn nhất",
    "Skewness",
    "Kurtosis",
    "Jarque-Bera p-value"
  ),
  `Giá trị` = c(
    format(length(r_vnindex), big.mark = "."),
    fmt_pct(mean(r_vnindex), 0.001),
    fmt_pct(sd(r_vnindex), 0.01),
    fmt_pct(min(r_vnindex), 0.1),
    fmt_pct(max(r_vnindex), 0.1),
    fmt_num(moments::skewness(r_vnindex), 3),
    fmt_num(moments::kurtosis(r_vnindex), 3),
    as.character(safe_pvalue(jb_test))
  )
)

desc_table |>
  knitr::kable(align = "lr")
```

Bảng @tbl-vnindex-descriptive-stats cho biết các đặc điểm phân phối cơ bản của tỷ lệ sinh lời VN-Index. Nếu kurtosis lớn hơn 3, phân phối return có đuôi dày hơn phân phối chuẩn. Nếu Jarque--Bera có p-value nhỏ, giả định phân phối chuẩn không phù hợp. Các kết quả này không tự động chỉ ra mô hình volatility nào tốt nhất, nhưng là cơ sở để cân nhắc các mô hình có phân phối Student-t hoặc phân phối bất đối xứng.

```{r}
#| label: fig-vnindex-hist-qq
#| fig-cap: "Phân phối tỷ lệ sinh lời VN-Index và so sánh với phân phối chuẩn"
#| fig-width: 8
#| fig-height: 5.8

mu_r <- mean(r_vnindex)
sd_r <- sd(r_vnindex)

df_hist <- df_price |>
  mutate(z = (return - mu_r) / sd_r)

p_hist <- ggplot(df_hist, aes(x = return)) +
  geom_histogram(aes(y = after_stat(density)), bins = 60, fill = "#D9EAF7", color = "white") +
  stat_function(fun = dnorm, args = list(mean = mu_r, sd = sd_r), linewidth = 0.9, color = book_color(2)) +
  scale_x_continuous(labels = label_pct(accuracy = 1)) +
  labs(
    title = "Histogram tỷ lệ sinh lời và đường chuẩn tham chiếu",
    x = "Tỷ lệ sinh lời",
    y = "Mật độ"
  ) +
  theme_book()

qq_df <- tibble(
  sample = sort(df_hist$z),
  theoretical = qnorm(ppoints(length(df_hist$z)))
)

p_qq <- ggplot(qq_df, aes(x = theoretical, y = sample)) +
  geom_point(size = 0.9, alpha = 0.65, color = book_color(1)) +
  geom_abline(intercept = 0, slope = 1, linetype = "dashed", color = book_color(2), linewidth = 0.7) +
  labs(
    title = "Q-Q plot của tỷ lệ sinh lời chuẩn hóa",
    x = "Phân vị chuẩn lý thuyết",
    y = "Phân vị mẫu"
  ) +
  theme_book()

print(p_hist)
print(p_qq)
```

Hình @fig-vnindex-hist-qq giúp kiểm tra trực quan tính chuẩn của tỷ lệ sinh lời. Nếu các điểm ở vùng đuôi của Q-Q plot lệch mạnh khỏi đường tham chiếu, dữ liệu có đuôi dày. Điều này có ý nghĩa trực tiếp với mô hình VaR vì vùng đuôi trái của phân phối return quyết định xác suất thua lỗ lớn.

```{r}
#| label: tbl-stationarity-tests
#| tbl-cap: "Kiểm định tính dừng cho log price và log return"

log_price <- df_price$log_price

adf_price <- safe_test(tseries::adf.test(log_price))
adf_return <- safe_test(tseries::adf.test(r_vnindex))
pp_price <- safe_test(tseries::pp.test(log_price))
pp_return <- safe_test(tseries::pp.test(r_vnindex))
kpss_price <- safe_test(tseries::kpss.test(log_price))
kpss_return <- safe_test(tseries::kpss.test(r_vnindex))

stationarity_table <- tibble(
  `Kiểm định` = c("ADF", "ADF", "PP", "PP", "KPSS", "KPSS"),
  `Biến` = c("log price", "log return", "log price", "log return", "log price", "log return"),
  `Thống kê` = c(
    safe_stat(adf_price), safe_stat(adf_return),
    safe_stat(pp_price), safe_stat(pp_return),
    safe_stat(kpss_price), safe_stat(kpss_return)
  ),
  `p-value` = c(
    safe_pvalue(adf_price), safe_pvalue(adf_return),
    safe_pvalue(pp_price), safe_pvalue(pp_return),
    safe_pvalue(kpss_price), safe_pvalue(kpss_return)
  )
)

stationarity_table |>
  knitr::kable(align = "llrr")
```

Bảng @tbl-stationarity-tests hỗ trợ lựa chọn biến mô hình. Trong phân tích volatility, chuỗi giá thường không được mô hình hóa trực tiếp vì có thể không dừng. Tỷ lệ sinh lời thường có tính dừng hơn và là biến phù hợp để ước lượng phương sai có điều kiện.

```{r}
#| label: fig-acf-return-squared-return
#| fig-cap: "ACF của tỷ lệ sinh lời và bình phương tỷ lệ sinh lời VN-Index"
#| fig-width: 8
#| fig-height: 4.8

acf_return <- make_acf_df(r_vnindex, "Return")
acf_sq_return <- make_acf_df(r_vnindex^2, "Squared return")

acf_df <- bind_rows(acf_return, acf_sq_return)

conf_band <- 1.96 / sqrt(length(r_vnindex))

ggplot(acf_df, aes(x = lag, y = acf)) +
  geom_col(width = 0.75, fill = book_color(1), alpha = 0.85) +
  geom_hline(yintercept = c(-conf_band, conf_band), linetype = "dashed", color = book_color(2), linewidth = 0.55) +
  facet_wrap(~ series, ncol = 1, scales = "free_y") +
  labs(
    title = "Phụ thuộc trong return và squared return",
    subtitle = "Tự tương quan trong squared return là dấu hiệu volatility clustering",
    x = "Độ trễ",
    y = "ACF"
  ) +
  theme_book()
```

Nếu ACF của return nhỏ nhưng ACF của squared return đáng kể, dữ liệu có thể không dễ dự báo ở trung bình nhưng lại có khả năng dự báo ở phương sai. Đây là lý do các mô hình volatility tập trung vào $
\sigma_t^2$ hơn là $
\mu_t$.

```{r}
#| label: tbl-ljung-arch-tests
#| tbl-cap: "Kiểm định tự tương quan và ARCH effect cho tỷ lệ sinh lời VN-Index"

lb_return <- Box.test(r_vnindex, lag = 12, type = "Ljung-Box")
lb_sq_return <- Box.test(r_vnindex^2, lag = 12, type = "Ljung-Box")
arch_lm <- FinTS::ArchTest(r_vnindex, lags = 12)

dep_table <- tibble(
  `Kiểm định` = c(
    "Ljung-Box trên return",
    "Ljung-Box trên return^2",
    "ARCH-LM"
  ),
  `Độ trễ` = c(12, 12, 12),
  `Thống kê` = c(
    round(unname(lb_return$statistic), 3),
    round(unname(lb_sq_return$statistic), 3),
    round(unname(arch_lm$statistic), 3)
  ),
  `p-value` = c(
    signif(lb_return$p.value, 4),
    signif(lb_sq_return$p.value, 4),
    signif(arch_lm$p.value, 4)
  )
)

dep_table |>
  knitr::kable(align = "lrrr")
```

Bảng @tbl-ljung-arch-tests là bước chẩn đoán quan trọng trước khi ước lượng GARCH. Nếu squared return có tự tương quan và ARCH-LM bác bỏ giả thuyết không, mô hình phương sai thay đổi theo thời gian có cơ sở thực nghiệm rõ ràng.

## Historical volatility và rolling volatility

Historical volatility là benchmark đơn giản nhất. Nó đo lường mức biến động trung bình trong toàn bộ mẫu hoặc trong một giai đoạn xác định. Trong khi đó, rolling volatility cho phép volatility thay đổi theo thời gian bằng cách sử dụng một cửa sổ dữ liệu gần hiện tại.

```{r}
#| label: tbl-historical-volatility
#| tbl-cap: "Historical volatility của VN-Index"

daily_vol <- sd(r_vnindex)
annual_vol <- daily_vol * sqrt(252)

hist_vol_table <- tibble(
  `Chỉ tiêu` = c(
    "Trung bình ngày",
    "Volatility ngày",
    "Volatility năm hóa",
    "Số ngày giao dịch"
  ),
  `Giá trị` = c(
    fmt_pct(mean(r_vnindex), 0.001),
    fmt_pct(daily_vol, 0.01),
    fmt_pct(annual_vol, 0.1),
    format(length(r_vnindex), big.mark = ".")
  )
)

hist_vol_table |>
  knitr::kable(align = "lr")
```

Bảng @tbl-historical-volatility là điểm khởi đầu của phân tích volatility. Tuy nhiên, một con số volatility toàn mẫu không cho biết rủi ro thay đổi như thế nào qua các giai đoạn thị trường. Vì vậy, cần bổ sung rolling volatility.

```{r}
#| label: fig-rolling-volatility-vnindex
#| fig-cap: "Rolling volatility của VN-Index theo các cửa sổ khác nhau"
#| fig-width: 8
#| fig-height: 4.8

windows <- c(20, 60, 120, 252)

df_rolling <- tibble(
  date = df_price$date,
  setNames(
    as.data.frame(lapply(windows, \(w) roll_sd(r_vnindex, w))),
    paste0("Cửa sổ ", windows, " ngày")
  )
) |>
  pivot_longer(-date, names_to = "window", values_to = "volatility")

ggplot(df_rolling, aes(x = date, y = volatility, color = window, linetype = window)) +
  geom_line(linewidth = 0.75, na.rm = TRUE) +
  scale_color_manual(values = book_palette) +
  scale_linetype_manual(values = book_linetypes) +
  scale_y_continuous(labels = label_pct(0.5)) +
  labs(
    title = "Rolling volatility phản ứng khác nhau tùy độ dài cửa sổ",
    subtitle = "Cửa sổ ngắn nhạy hơn với cú sốc; cửa sổ dài mượt hơn nhưng phản ứng chậm",
    x = "Thời gian",
    y = "Volatility ngày",
    color = "Cửa sổ",
    linetype = "Cửa sổ"
  ) +
  theme_book()
```

Hình @fig-rolling-volatility-vnindex cho phép nhìn thấy các giai đoạn thị trường biến động cao và thấp. Cửa sổ 20 ngày thường phản ứng nhanh với cú sốc gần nhất. Cửa sổ 252 ngày phản ánh xu hướng volatility dài hơn nhưng có độ trễ đáng kể.

```{r}
#| label: tbl-rolling-volatility-summary
#| tbl-cap: "Tóm tắt rolling volatility theo độ dài cửa sổ"

rolling_summary <- df_rolling |>
  filter(is.finite(volatility)) |>
  group_by(window) |>
  summarise(
    `Trung bình` = fmt_pct(mean(volatility), 0.01),
    `Trung vị` = fmt_pct(median(volatility), 0.01),
    `Phân vị 95%` = fmt_pct(quantile(volatility, 0.95), 0.01),
    `Lớn nhất` = fmt_pct(max(volatility), 0.01),
    .groups = "drop"
  )

rolling_summary |>
  knitr::kable(align = "lrrrr")
```

Bảng @tbl-rolling-volatility-summary cho thấy mức độ nhạy của rolling volatility đối với lựa chọn cửa sổ. Khi sử dụng rolling volatility trong quản trị rủi ro, lựa chọn cửa sổ không phải là vấn đề kỹ thuật thuần túy mà ảnh hưởng trực tiếp đến mức rủi ro được báo cáo.

## EWMA/RiskMetrics

EWMA mở rộng rolling volatility bằng cách gán trọng số lớn hơn cho các quan sát gần hiện tại. Trong thực hành quản trị rủi ro, giá trị $
\lambda = 0.94$ thường được xem là lựa chọn chuẩn cho dữ liệu ngày trong khuôn khổ RiskMetrics [@RiskMetrics1996]. Tuy nhiên, với dữ liệu thực tế, nên kiểm tra độ nhạy của kết quả với các giá trị $
\lambda$ khác nhau.

```{r}
#| label: fig-ewma-vnindex-lambda
#| fig-cap: "EWMA volatility của VN-Index theo các giá trị lambda"
#| fig-width: 8
#| fig-height: 4.8

lambda_values <- c(0.90, 0.94, 0.97)

df_ewma <- tibble(
  date = df_price$date,
  setNames(
    as.data.frame(lapply(lambda_values, \(lambda) ewma_vol(r_vnindex, lambda))),
    paste0("lambda = ", lambda_values)
  )
) |>
  pivot_longer(-date, names_to = "lambda", values_to = "volatility")

ggplot(df_ewma, aes(x = date, y = volatility, color = lambda, linetype = lambda)) +
  geom_line(linewidth = 0.75, na.rm = TRUE) +
  scale_color_manual(values = book_palette) +
  scale_linetype_manual(values = book_linetypes) +
  scale_y_continuous(labels = label_pct(0.5)) +
  labs(
    title = "EWMA volatility phụ thuộc vào hệ số suy giảm lambda",
    subtitle = "Lambda lớn làm volatility mượt hơn nhưng phản ứng chậm hơn với cú sốc mới",
    x = "Thời gian",
    y = "EWMA volatility ngày",
    color = "Hệ số lambda",
    linetype = "Hệ số lambda"
  ) +
  theme_book()
```

Hình @fig-ewma-vnindex-lambda minh họa vai trò của hệ số suy giảm. Khi $
\lambda$ thấp, mô hình phản ứng nhanh hơn với thông tin mới. Khi $
\lambda$ cao, cú sốc quá khứ tồn tại lâu hơn trong volatility ước lượng.

```{r}
#| label: tbl-ewma-summary
#| tbl-cap: "Tóm tắt EWMA volatility theo lambda"

ewma_summary <- df_ewma |>
  filter(is.finite(volatility)) |>
  group_by(lambda) |>
  summarise(
    `Trung bình` = fmt_pct(mean(volatility), 0.01),
    `Trung vị` = fmt_pct(median(volatility), 0.01),
    `Phân vị 95%` = fmt_pct(quantile(volatility, 0.95), 0.01),
    `Lớn nhất` = fmt_pct(max(volatility), 0.01),
    .groups = "drop"
  )

ewma_summary |>
  knitr::kable(align = "lrrrr")
```

Bảng @tbl-ewma-summary giúp so sánh mức volatility ước lượng dưới các giá trị $
\lambda$ khác nhau. EWMA có ưu điểm là cập nhật liên tục hơn rolling volatility, nhưng vẫn thiếu cơ chế quay về mức phương sai dài hạn như GARCH.

```{r}
#| label: fig-ewma-rolling-comparison-vnindex
#| fig-cap: "So sánh rolling volatility và EWMA volatility của VN-Index"
#| fig-width: 8
#| fig-height: 4.8

df_rolling_ewma <- tibble(
  date = df_price$date,
  `Rolling 60 ngày` = roll_sd(r_vnindex, 60),
  `EWMA lambda = 0.94` = ewma_vol(r_vnindex, 0.94)
) |>
  pivot_longer(-date, names_to = "method", values_to = "volatility")

ggplot(df_rolling_ewma, aes(x = date, y = volatility, color = method, linetype = method)) +
  geom_line(linewidth = 0.8, na.rm = TRUE) +
  scale_color_manual(values = book_palette) +
  scale_linetype_manual(values = book_linetypes) +
  scale_y_continuous(labels = label_pct(0.5)) +
  labs(
    title = "EWMA cập nhật volatility liên tục hơn rolling volatility",
    subtitle = "Rolling volatility có thể thay đổi đột ngột khi quan sát cực đoan rơi khỏi cửa sổ",
    x = "Thời gian",
    y = "Volatility ngày",
    color = "Phương pháp",
    linetype = "Phương pháp"
  ) +
  theme_book()
```

Hình @fig-ewma-rolling-comparison-vnindex cho thấy EWMA và rolling volatility có thể đưa ra tín hiệu khác nhau trong các giai đoạn biến động. Đây là lý do phần dự báo ở cuối chương không chỉ dựa vào một phương pháp duy nhất, mà so sánh nhiều mô hình với cùng một bộ tiêu chí.

## ARCH/GARCH và các mô hình mở rộng

Các kết quả ở trên cho thấy volatility của VN-Index thay đổi theo thời gian. Tuy nhiên, historical volatility, rolling volatility và EWMA chưa mô hình hóa chính thức phương sai có điều kiện. Nhóm mô hình ARCH/GARCH cho phép volatility hiện tại phụ thuộc vào cú sốc quá khứ và volatility quá khứ. Đây là nền tảng quan trọng cho dự báo volatility động [@Engle1982; @Bollerslev1986].

### Ước lượng các mô hình GARCH-type

Trong chương này, các mô hình GARCH-type được ước lượng với phương trình trung bình đơn giản và phân phối sai số chuẩn hoặc Student-t. Phân phối Student-t được sử dụng vì tỷ lệ sinh lời tài chính thường có đuôi dày hơn phân phối chuẩn. Bộ mô hình được xét gồm GARCH-normal, GARCH-Student-t, GJR-GARCH-Student-t và EGARCH-Student-t.

```{r}
#| label: fit-garch-models-vnindex
#| include: false

fit_garch_norm <- fit_garch_safe(r_vnindex, model = "sGARCH", dist = "norm")
fit_garch_std <- fit_garch_safe(r_vnindex, model = "sGARCH", dist = "std")
fit_gjr_std <- fit_garch_safe(r_vnindex, model = "gjrGARCH", dist = "std")
fit_egarch_std <- fit_garch_safe(r_vnindex, model = "eGARCH", dist = "std")

model_fits <- list(
  "GARCH-normal" = fit_garch_norm,
  "GARCH-Student-t" = fit_garch_std,
  "GJR-GARCH-Student-t" = fit_gjr_std,
  "EGARCH-Student-t" = fit_egarch_std
)

model_success <- !vapply(model_fits, is.null, logical(1))
model_fits_ok <- model_fits[model_success]
```

```{r}
#| label: tbl-garch-ic-comparison
#| tbl-cap: "So sánh AIC/BIC của các mô hình GARCH-type"

ic_comparison <- bind_rows(
  lapply(names(model_fits), function(nm) ic_table(model_fits[[nm]], nm))
)

ic_comparison |>
  knitr::kable(align = "lrrr")
```

Bảng @tbl-garch-ic-comparison so sánh mức độ phù hợp trong mẫu của các mô hình. AIC và BIC thấp hơn thường cho thấy mô hình phù hợp hơn sau khi đã phạt số lượng tham số. Tuy nhiên, lựa chọn mô hình không nên chỉ dựa vào AIC/BIC; cần kiểm tra phần dư chuẩn hóa và hiệu quả dự báo ngoài mẫu.

```{r}
#| label: tbl-garch-coefficients
#| tbl-cap: "Hệ số ước lượng của các mô hình GARCH-type"

garch_coef_journal <- garch_journal_table(model_fits)

garch_coef_journal |>
  knitr::kable(
    align = paste0("l", paste(rep("r", ncol(garch_coef_journal) - 1), collapse = ""))
  )
```

Bảng @tbl-garch-coefficients trình bày kết quả theo dạng cột kiểu bài báo: mỗi cột là một mô hình, mỗi ô gồm hệ số ước lượng, ký hiệu mức ý nghĩa thống kê và sai số chuẩn trong ngoặc. Ký hiệu *, ** và *** lần lượt tương ứng với mức ý nghĩa 10%, 5% và 1%. Trong GARCH chuẩn, `alpha1` phản ánh tác động của cú sốc mới, còn `beta1` phản ánh độ bền của volatility. Trong GJR-GARCH, `gamma1` đo hiệu ứng bất đối xứng. Trong EGARCH, hệ số liên quan đến dấu của shock cho biết phản ứng khác nhau giữa cú sốc âm và cú sốc dương.

```{r}
#| label: tbl-garch-persistence
#| tbl-cap: "Persistence và half-life của mô hình GARCH-type"

get_persistence <- function(fit, model_name) {
  if (is.null(fit)) {
    return(tibble(
      `Mô hình` = model_name,
      `Persistence` = NA_real_,
      `Half-life` = NA_real_
    ))
  }

  co <- coef(fit)

  if ("alpha1" %in% names(co) && "beta1" %in% names(co)) {
    if ("gamma1" %in% names(co) && grepl("GJR", model_name)) {
      persistence <- as.numeric(co["alpha1"] + 0.5 * co["gamma1"] + co["beta1"])
    } else {
      persistence <- as.numeric(co["alpha1"] + co["beta1"])
    }
  } else {
    persistence <- NA_real_
  }

  half_life <- ifelse(
    is.finite(persistence) && persistence > 0 && persistence < 1,
    log(0.5) / log(persistence),
    NA_real_
  )

  tibble(
    `Mô hình` = model_name,
    `Persistence` = round(persistence, 4),
    `Half-life` = round(half_life, 2)
  )
}

persistence_table <- bind_rows(
  lapply(names(model_fits), function(nm) get_persistence(model_fits[[nm]], nm))
)

persistence_table |>
  knitr::kable(align = "lrr")
```

Bảng @tbl-garch-persistence cho biết mức độ bền vững của volatility. Persistence càng gần 1, cú sốc volatility càng mất nhiều thời gian để triệt tiêu. Đây là thông tin quan trọng khi sử dụng mô hình để dự báo volatility nhiều ngày tới.

```{r}
#| label: fig-conditional-volatility-garch
#| fig-cap: "Volatility có điều kiện từ các mô hình GARCH-type"
#| fig-width: 8
#| fig-height: 4.8

sigma_list <- lapply(names(model_fits_ok), function(nm) {
  tibble(
    date = df_price$date,
    model = nm,
    volatility = as.numeric(sigma(model_fits_ok[[nm]]))
  )
})

df_sigma_models <- bind_rows(sigma_list)

ggplot(df_sigma_models, aes(x = date, y = volatility, color = model, linetype = model)) +
  geom_line(linewidth = 0.68, na.rm = TRUE) +
  scale_color_manual(values = book_palette) +
  scale_linetype_manual(values = book_linetypes) +
  scale_y_continuous(labels = label_pct(0.5)) +
  labs(
    title = "Các mô hình GARCH-type tạo ra volatility có điều kiện khác nhau",
    subtitle = "Khác biệt rõ hơn trong các giai đoạn thị trường có biến động mạnh",
    x = "Thời gian",
    y = "Volatility có điều kiện",
    color = "Mô hình",
    linetype = "Mô hình"
  ) +
  theme_book()
```

Hình @fig-conditional-volatility-garch cho thấy volatility ước lượng từ các mô hình khác nhau. Nếu các mô hình cho kết quả gần nhau, GARCH chuẩn có thể đủ tốt cho mục tiêu mô tả. Nếu mô hình bất đối xứng tạo ra volatility cao hơn trong các giai đoạn giảm mạnh, điều này cho thấy hiệu ứng leverage có thể quan trọng đối với VN-Index.

```{r}
#| label: fig-news-impact-vnindex
#| fig-cap: "News impact curve minh họa phản ứng bất đối xứng của GJR-GARCH"
#| fig-width: 8
#| fig-height: 4.8

if (!is.null(fit_gjr_std)) {
  co_gjr <- coef(fit_gjr_std)
  shock_grid <- seq(
    quantile(r_vnindex, 0.01, na.rm = TRUE),
    quantile(r_vnindex, 0.99, na.rm = TRUE),
    length.out = 300
  )

  sigma2_bar <- mean(sigma(fit_gjr_std)^2, na.rm = TRUE)

  news_impact <- co_gjr["omega"] +
    co_gjr["alpha1"] * shock_grid^2 +
    co_gjr["gamma1"] * shock_grid^2 * (shock_grid < 0) +
    co_gjr["beta1"] * sigma2_bar

  df_news <- tibble(
    shock = shock_grid,
    next_variance = as.numeric(news_impact)
  )

  ggplot(df_news, aes(x = shock, y = sqrt(pmax(next_variance, 0)))) +
    geom_line(linewidth = 0.95, color = book_color(2)) +
    geom_vline(xintercept = 0, linetype = "dashed", color = "grey35") +
    scale_x_continuous(labels = label_pct(accuracy = 1)) +
    scale_y_continuous(labels = label_pct(accuracy = 0.5)) +
    labs(
      title = "Cú sốc âm có thể làm volatility tăng mạnh hơn cú sốc dương",
      subtitle = "Đường cong được tính từ hệ số GJR-GARCH ước lượng cho VN-Index",
      x = "Cú sốc tỷ lệ sinh lời",
      y = "Volatility kỳ tiếp theo"
    ) +
    theme_book()
} else {
  ggplot() +
    annotate(
      "text", x = 0, y = 0,
      label = "Không ước lượng được GJR-GARCH nên không vẽ news impact curve.",
      family = base_family
    ) +
    theme_void(base_family = base_family)
}
```

Hình @fig-news-impact-vnindex trực quan hóa vai trò của mô hình bất đối xứng. Nếu phần bên trái của đường cong cao hơn bên phải, cú sốc âm làm volatility kỳ sau tăng mạnh hơn cú sốc dương có cùng độ lớn tuyệt đối.

### Kiểm định chẩn đoán phần dư chuẩn hóa

Sau khi ước lượng GARCH-type models, cần kiểm tra standardized residuals. Một mô hình phù hợp không nên để lại tự tương quan đáng kể trong phần dư hoặc bình phương phần dư chuẩn hóa.

```{r}
#| label: tbl-garch-diagnostics-vnindex
#| tbl-cap: "Chẩn đoán phần dư chuẩn hóa của các mô hình GARCH-type"

make_diag_table <- function(fit, model_name) {
  if (is.null(fit)) {
    return(tibble(
      `Mô hình` = model_name,
      `Kiểm định` = NA_character_,
      `Thống kê` = NA_real_,
      `p-value` = NA_real_
    ))
  }

  z <- as.numeric(residuals(fit, standardize = TRUE))
  z <- z[is.finite(z)]

  tests <- list(
    "Ljung-Box trên z" = Box.test(z, lag = 12, type = "Ljung-Box"),
    "Ljung-Box trên z^2" = Box.test(z^2, lag = 12, type = "Ljung-Box"),
    "ARCH-LM trên z" = FinTS::ArchTest(z, lags = 12)
  )

  tibble(
    `Mô hình` = model_name,
    `Kiểm định` = names(tests),
    `Thống kê` = round(vapply(tests, \(x) unname(x$statistic), numeric(1)), 3),
    `p-value` = signif(vapply(tests, \(x) x$p.value, numeric(1)), 4)
  )
}

diag_table <- bind_rows(
  lapply(names(model_fits), function(nm) make_diag_table(model_fits[[nm]], nm))
)

diag_table |>
  knitr::kable(align = "llrr")
```

Bảng @tbl-garch-diagnostics-vnindex giúp đánh giá xem mô hình đã xử lý được cấu trúc phụ thuộc trong volatility hay chưa. Nếu `Ljung-Box trên z^2` hoặc `ARCH-LM trên z` vẫn có p-value nhỏ, mô hình có thể chưa mô tả hết phương sai có điều kiện.

## Các thước đo volatility từ dữ liệu OHLC

Ngoài close-to-close volatility, dữ liệu OHLC cho phép xây dựng các thước đo volatility sử dụng thông tin dao động nội ngày. Các thước đo này không thay thế hoàn toàn GARCH, nhưng có thể đóng vai trò proxy cho realized variance khi đánh giá forecast volatility.

Parkinson volatility sử dụng khoảng cách giữa giá cao nhất và thấp nhất trong ngày:

$$
\sigma^2_{P,t}
=
\frac{1}{4\log(2)}
\left[\log\left(\frac{H_t}{L_t}\right)\right]^2.
$$

Garman--Klass volatility sử dụng thêm thông tin Open và Close:

$$
\sigma^2_{GK,t}
=
0.5\left[\log\left(\frac{H_t}{L_t}\right)\right]^2
-
(2\log 2-1)
\left[\log\left(\frac{C_t}{O_t}\right)\right]^2.
$$

```{r}
#| label: compute-range-based-volatility
#| include: false

df_range_vol <- df_price |>
  mutate(
    log_hl = log(high / low),
    log_co = log(close / open),
    cc_var = return^2,
    parkinson_var = (log_hl^2) / (4 * log(2)),
    gk_var = 0.5 * log_hl^2 - (2 * log(2) - 1) * log_co^2,
    parkinson_var = pmax(parkinson_var, 0),
    gk_var = pmax(gk_var, 0)
  )
```

```{r}
#| label: fig-range-based-volatility
#| fig-cap: "Range-based volatility của VN-Index từ dữ liệu OHLC"
#| fig-width: 8
#| fig-height: 4.8

range_plot_data <- df_range_vol |>
  mutate(
    `Close-to-close` = zoo::rollapplyr(cc_var, 20, mean, fill = NA),
    `Parkinson` = zoo::rollapplyr(parkinson_var, 20, mean, fill = NA),
    `Garman-Klass` = zoo::rollapplyr(gk_var, 20, mean, fill = NA)
  ) |>
  transmute(
    date,
    `Close-to-close` = sqrt(`Close-to-close`),
    Parkinson = sqrt(Parkinson),
    `Garman-Klass` = sqrt(`Garman-Klass`)
  ) |>
  pivot_longer(-date, names_to = "method", values_to = "volatility")

ggplot(range_plot_data, aes(x = date, y = volatility, color = method, linetype = method)) +
  geom_line(linewidth = 0.75, na.rm = TRUE) +
  scale_color_manual(values = book_palette) +
  scale_linetype_manual(values = book_linetypes) +
  scale_y_continuous(labels = label_pct(0.5)) +
  labs(
    title = "OHLC bổ sung thông tin nội ngày cho đo lường volatility",
    subtitle = "Các thước đo range-based có thể khác với close-to-close volatility",
    x = "Thời gian",
    y = "Volatility ngày, rolling 20 ngày",
    color = "Thước đo",
    linetype = "Thước đo"
  ) +
  theme_book()
```

Hình @fig-range-based-volatility cho thấy range-based volatility có thể cung cấp tín hiệu khác với volatility tính từ close-to-close return. Trong phần đánh giá dự báo, Garman--Klass variance được dùng như một proxy bổ sung cho realized variance ở horizon 1 ngày. Với horizon dài hơn, chương sử dụng tổng bình phương return trong các ngày tương lai làm proxy thống nhất.

```{r}
#| label: tbl-volatility-measure-comparison
#| tbl-cap: "So sánh các nhóm thước đo volatility trong ứng dụng VN-Index"

vol_measure_comparison <- tibble(
  `Nhóm thước đo` = c(
    "Historical",
    "Rolling",
    "EWMA",
    "GARCH-type",
    "Range-based"
  ),
  `Dữ liệu cần` = c(
    "Close",
    "Close",
    "Close",
    "Close",
    "OHLC"
  ),
  `Vai trò trong chương` = c(
    "Benchmark",
    "Dynamic benchmark",
    "RiskMetrics benchmark",
    "Mô hình chính",
    "Proxy bổ sung"
  )
)

vol_measure_comparison |>
  knitr::kable(align = "lll")
```

## Dự báo volatility theo nhiều horizon

Phần này đánh giá khả năng dự báo volatility của các mô hình theo các horizon 1, 5, 10 và 20 ngày. Cần phân biệt giữa **fitted volatility** và **forecast volatility**. Fitted volatility là volatility ước lượng trong mẫu. Forecast volatility là volatility được dự báo tại thời điểm $t$ cho các kỳ tương lai, ký hiệu chung là $\hat{\sigma}_{t+h|t}$.

Với dữ liệu thực, volatility thật không quan sát trực tiếp. Vì vậy, đánh giá forecast cần một proxy. Với horizon $h$, chương sử dụng tổng bình phương return trong $h$ ngày tiếp theo:

$$
\widetilde{\sigma}_{t,h}^{2}
=
\sum_{j=1}^{h} r_{t+j}^2.
$$

Đây là proxy đơn giản, thống nhất cho nhiều horizon. Nếu có dữ liệu intraday, có thể thay bằng realized variance tần suất cao. Nếu dùng dữ liệu OHLC, có thể dùng range-based variance cho horizon 1 ngày như một kiểm tra bổ sung.

### Thiết kế forecast exercise

Forecast exercise sử dụng rolling estimation window. Tại mỗi forecast origin, mô hình được ước lượng lại trên một cửa sổ dữ liệu gần nhất, sau đó dự báo volatility cho các horizon 1, 5, 10 và 20 ngày. Để thời gian render hợp lý, file QMD mặc định refit mô hình cách nhau 20 ngày giao dịch và giới hạn số forecast origins gần cuối mẫu.

```{r}
#| label: tbl-forecast-exercise-design
#| tbl-cap: "Thiết kế forecast exercise"

estimation_window <- min(1250, max(750, floor(length(r_vnindex) * 0.55)))
horizons <- c(1, 5, 10, 20)
forecast_step <- 20
max_forecast_origins <- 50

forecast_exercise_table <- tibble(
  `Thành phần` = c(
    "Estimation window",
    "Forecast horizons",
    "Refit frequency",
    "Số forecast origins tối đa",
    "Proxy volatility thực tế",
    "Loss functions"
  ),
  `Thiết lập` = c(
    paste0(estimation_window, " ngày giao dịch"),
    "1, 5, 10, 20 ngày",
    paste0("Mỗi ", forecast_step, " ngày"),
    as.character(max_forecast_origins),
    "Tổng bình phương return tương lai",
    "RMSE, MAE, QLIKE"
  )
)

forecast_exercise_table |>
  knitr::kable(align = "ll")
```

```{r}
#| label: volatility-forecast-exercise
#| include: false
#| cache: true

n_obs <- length(r_vnindex)
max_h <- max(horizons)

start_origin <- estimation_window
end_origin <- n_obs - max_h

forecast_origins <- seq(start_origin, end_origin, by = forecast_step)

if (length(forecast_origins) > max_forecast_origins) {
  forecast_origins <- tail(forecast_origins, max_forecast_origins)
}

forecast_rows <- list()
row_id <- 1

for (origin in forecast_origins) {
  train_index <- (origin - estimation_window + 1):origin
  x_train <- r_vnindex[train_index]

  current_date <- df_price$date[origin]

  # Benchmark rolling và EWMA
  var_roll_60 <- var(tail(x_train, 60), na.rm = TRUE)
  var_roll_120 <- var(tail(x_train, 120), na.rm = TRUE)
  var_ewma <- tail(ewma_vol(x_train, lambda = 0.94)^2, 1)

  for (h in horizons) {
    actual_var <- sum(r_vnindex[(origin + 1):(origin + h)]^2, na.rm = TRUE)

    forecast_rows[[row_id]] <- tibble(
      date = current_date,
      horizon = h,
      model = "Rolling 60",
      actual_var = actual_var,
      forecast_var = h * var_roll_60
    )
    row_id <- row_id + 1

    forecast_rows[[row_id]] <- tibble(
      date = current_date,
      horizon = h,
      model = "Rolling 120",
      actual_var = actual_var,
      forecast_var = h * var_roll_120
    )
    row_id <- row_id + 1

    forecast_rows[[row_id]] <- tibble(
      date = current_date,
      horizon = h,
      model = "EWMA 0.94",
      actual_var = actual_var,
      forecast_var = h * var_ewma
    )
    row_id <- row_id + 1
  }

  # GARCH-type forecasts
  fit_o_garch <- fit_garch_safe(x_train, model = "sGARCH", dist = "std")
  fit_o_gjr <- fit_garch_safe(x_train, model = "gjrGARCH", dist = "std")
  fit_o_egarch <- fit_garch_safe(x_train, model = "eGARCH", dist = "std")

  forecast_model_list <- list(
    "GARCH-Student-t" = fit_o_garch,
    "GJR-GARCH-Student-t" = fit_o_gjr,
    "EGARCH-Student-t" = fit_o_egarch
  )

  for (model_name in names(forecast_model_list)) {
    fit_obj <- forecast_model_list[[model_name]]

    if (!is.null(fit_obj)) {
      fc <- tryCatch(
        rugarch::ugarchforecast(fit_obj, n.ahead = max_h),
        error = function(e) NULL
      )

      if (!is.null(fc)) {
        sigma2_path <- as.numeric(sigma(fc))^2

        for (h in horizons) {
          actual_var <- sum(r_vnindex[(origin + 1):(origin + h)]^2, na.rm = TRUE)

          forecast_rows[[row_id]] <- tibble(
            date = current_date,
            horizon = h,
            model = model_name,
            actual_var = actual_var,
            forecast_var = sum(sigma2_path[1:h], na.rm = TRUE)
          )
          row_id <- row_id + 1
        }
      }
    }
  }
}

df_forecast_eval <- bind_rows(forecast_rows) |>
  filter(is.finite(actual_var), is.finite(forecast_var), forecast_var > 0)
```

```{r}
#| label: forecast-loss-objects
#| include: false

forecast_loss_raw <- df_forecast_eval |>
  group_by(horizon, model) |>
  summarise(
    RMSE = sqrt(mean((forecast_var - actual_var)^2, na.rm = TRUE)),
    MAE = mean(abs(forecast_var - actual_var), na.rm = TRUE),
    QLIKE = qlike_loss(actual_var, forecast_var),
    .groups = "drop"
  ) |>
  arrange(horizon, QLIKE)

format_loss_value <- function(x) {
  ifelse(
    is.na(x),
    "",
    formatC(x, format = "e", digits = 3)
  )
}

make_metric_panel <- function(metric_name) {
  forecast_loss_raw |>
    select(model, horizon, value = all_of(metric_name)) |>
    mutate(
      horizon = paste0("h = ", horizon),
      value = format_loss_value(value)
    ) |>
    tidyr::pivot_wider(
      names_from = horizon,
      values_from = value
    ) |>
    arrange(model)
}

best_by_metric <- forecast_loss_raw |>
  pivot_longer(
    cols = c(RMSE, MAE, QLIKE),
    names_to = "metric",
    values_to = "value"
  ) |>
  group_by(horizon, metric) |>
  slice_min(value, n = 1, with_ties = FALSE) |>
  ungroup() |>
  select(horizon, metric, model) |>
  mutate(horizon = paste0("h = ", horizon)) |>
  pivot_wider(
    names_from = metric,
    values_from = model
  ) |>
  arrange(horizon)
```

```{r}
#| label: tbl-volatility-forecast-rmse
#| tbl-cap: "Forecast comparison theo RMSE"

make_metric_panel("RMSE") |>
  knitr::kable(
    align = "lrrrr",
    booktabs = TRUE
  )
```

```{r}
#| label: tbl-volatility-forecast-mae
#| tbl-cap: "Forecast comparison theo MAE"

make_metric_panel("MAE") |>
  knitr::kable(
    align = "lrrrr",
    booktabs = TRUE
  )
```

```{r}
#| label: tbl-volatility-forecast-qlike
#| tbl-cap: "Forecast comparison theo QLIKE"

make_metric_panel("QLIKE") |>
  knitr::kable(
    align = "lrrrr",
    booktabs = TRUE
  )
```

Các Bảng @tbl-volatility-forecast-rmse, @tbl-volatility-forecast-mae và @tbl-volatility-forecast-qlike trình bày kết quả theo phong cách journal-style: mỗi panel tương ứng với một tiêu chí loss function, mỗi dòng là một mô hình và mỗi cột là một forecast horizon. Cách trình bày này giúp so sánh mô hình theo chiều ngang rõ hơn so với bảng dài dạng ``model--horizon--metric''. RMSE phạt mạnh các sai số lớn; MAE dễ diễn giải hơn; QLIKE thường được dùng trong đánh giá forecast variance vì nhạy với việc đánh giá thấp volatility. Một mô hình có thể tốt ở horizon ngắn nhưng không nhất thiết tốt ở horizon dài.

```{r}
#| label: tbl-best-model-by-horizon
#| tbl-cap: "Mô hình tốt nhất theo từng tiêu chí và horizon"

best_by_metric |>
  knitr::kable(
    align = "llll",
    booktabs = TRUE
  )
```

Bảng @tbl-best-model-by-horizon tổng hợp mô hình tốt nhất theo từng tiêu chí. Nếu các tiêu chí khác nhau chọn các mô hình khác nhau, nhà phân tích cần ưu tiên tiêu chí phù hợp với mục tiêu sử dụng. Đối với quản trị rủi ro, QLIKE thường đáng chú ý vì đánh giá trực tiếp forecast variance; tuy nhiên, RMSE và MAE vẫn hữu ích để kiểm tra độ nhạy của kết luận.

```{r}
#| label: fig-forecast-vs-proxy
#| fig-cap: "Forecast volatility và proxy volatility thực tế ở horizon 1 ngày"
#| fig-width: 8
#| fig-height: 4.8

plot_horizon <- 1
selected_models <- c("Rolling 60", "EWMA 0.94", "GARCH-Student-t", "GJR-GARCH-Student-t")

forecast_plot_data <- df_forecast_eval |>
  filter(horizon == plot_horizon, model %in% selected_models) |>
  mutate(
    actual_vol = sqrt(actual_var),
    forecast_vol = sqrt(forecast_var)
  )

actual_plot <- forecast_plot_data |>
  distinct(date, actual_vol) |>
  mutate(model = "Proxy thực tế", volatility = actual_vol)

forecast_plot <- forecast_plot_data |>
  transmute(date, model, volatility = forecast_vol)

combined_forecast_plot <- bind_rows(actual_plot, forecast_plot)

ggplot(combined_forecast_plot, aes(x = date, y = volatility, color = model, linetype = model)) +
  geom_line(linewidth = 0.75, na.rm = TRUE) +
  scale_color_manual(values = book_palette) +
  scale_linetype_manual(values = book_linetypes) +
  scale_y_continuous(labels = label_pct(accuracy = 1)) +
  labs(
    title = "So sánh forecast volatility với proxy volatility thực tế",
    subtitle = "Proxy thực tế được tính bằng căn bậc hai của squared return tương lai",
    x = "Forecast origin",
    y = "Volatility",
    color = "Chuỗi",
    linetype = "Chuỗi"
  ) +
  theme_book()
```

Hình @fig-forecast-vs-proxy giúp đánh giá trực quan mô hình nào phản ứng quá chậm, mô hình nào quá nhiễu, và mô hình nào bám sát proxy volatility hơn trong giai đoạn out-of-sample. Vì proxy volatility dựa trên squared return rất nhiễu, kết quả nên được đọc cùng bảng loss functions thay vì chỉ nhìn hình.

```{r}
#| label: fig-garch-multistep-forecast
#| fig-cap: "Minh họa multi-step volatility forecast từ GARCH-Student-t"
#| fig-width: 8
#| fig-height: 4.8

last_window <- tail(r_vnindex, estimation_window)
last_fit <- fit_garch_safe(last_window, model = "sGARCH", dist = "std")

if (!is.null(last_fit)) {
  last_fc <- rugarch::ugarchforecast(last_fit, n.ahead = 20)

  df_multistep <- tibble(
    horizon = 1:20,
    forecast_volatility = as.numeric(sigma(last_fc))
  )

  ggplot(df_multistep, aes(x = horizon, y = forecast_volatility)) +
    geom_line(linewidth = 0.95, color = book_color(1)) +
    geom_point(size = 1.8, color = book_color(2)) +
    scale_y_continuous(labels = label_pct(accuracy = 0.5)) +
    scale_x_continuous(breaks = c(1, 5, 10, 15, 20)) +
    labs(
      title = "Dự báo volatility nhiều bước từ GARCH",
      subtitle = "Forecast nhiều bước cho thấy xu hướng hội tụ về mức volatility dài hạn của mô hình",
      x = "Horizon dự báo, ngày",
      y = "Volatility dự báo"
    ) +
    theme_book()
} else {
  ggplot() +
    annotate(
      "text", x = 0, y = 0,
      label = "Không ước lượng được GARCH nên không vẽ multi-step forecast.",
      family = base_family
    ) +
    theme_void(base_family = base_family)
}
```

Hình @fig-garch-multistep-forecast minh họa trực quan forecast horizon. Khác với fitted volatility, multi-step forecast cho biết mô hình dự báo volatility tương lai như thế nào khi không có thêm thông tin mới. Với GARCH dừng, dự báo nhiều bước thường có xu hướng tiến dần về mức volatility dài hạn.

## Thảo luận kết quả và hàm ý quản trị rủi ro

Kết quả trong chương này cần được đọc theo ba lớp. Thứ nhất, các kiểm định ban đầu cho biết tỷ lệ sinh lời VN-Index có đặc điểm phù hợp với mô hình volatility động hay không. Nếu squared return có tự tương quan và ARCH-LM có ý nghĩa thống kê, mô hình GARCH-type có cơ sở thực nghiệm.

Thứ hai, các mô hình đo lường volatility khác nhau phản ứng khác nhau với cú sốc thị trường. Rolling volatility dễ hiểu nhưng phụ thuộc mạnh vào lựa chọn cửa sổ. EWMA phản ứng liên tục hơn nhưng không có mean reversion rõ ràng. GARCH-type models có nền tảng kinh tế lượng tốt hơn, nhưng cần kiểm định phần dư và lựa chọn phân phối phù hợp.

Thứ ba, hiệu quả forecast không nên đánh giá bằng một tiêu chí duy nhất. RMSE, MAE và QLIKE phản ánh các khía cạnh khác nhau của sai số dự báo. Đối với quản trị rủi ro, việc đánh giá thấp volatility trong giai đoạn thị trường căng thẳng có thể nghiêm trọng hơn việc đánh giá cao volatility trong giai đoạn bình ổn. Do đó, mô hình được chọn cần phù hợp với mục tiêu sử dụng: mô tả rủi ro, cảnh báo sớm, VaR ngắn hạn, hay stress testing.

## Kết luận chương

Chương này đã áp dụng hệ thống phương pháp ở Chương 2 để đo lường và dự báo volatility của VN-Index. Quy trình thực nghiệm bắt đầu từ mô tả dữ liệu và kiểm định đặc điểm return, sau đó triển khai historical volatility, rolling volatility, EWMA, GARCH-type models, range-based volatility và forecast evaluation theo nhiều horizon.

Kết quả của chương cung cấp nền tảng cho bước tiếp theo trong đo lường rủi ro thị trường: ước lượng VaR và Expected Shortfall. Volatility forecast không phải là mục tiêu cuối cùng, mà là đầu vào trung tâm để xây dựng phân phối lỗ có điều kiện. Chương tiếp theo sẽ sử dụng các dự báo volatility và giả định phân phối phù hợp để tính toán VaR, kiểm định backtesting và đánh giá hàm ý quản trị rủi ro cho thị trường Việt Nam.
